{
  "name": "@toolkit/test",
  "version": "0.0.1",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "scripts": {
    "copy": "cp package-lock.json README.md dist",
    "build": "rm -rf dist && tsc -p tsconfig.json && npm run copy"
  },
  "devDependencies": {
    "typescript": "^3.8.3"
  }
}
